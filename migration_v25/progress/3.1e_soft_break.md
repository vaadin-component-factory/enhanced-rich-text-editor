# Phase 3.1e: Soft-Break + Tab Copying (Feature 3)

## Status: NOT STARTED

## Feature Summary
Shift+Enter inserts visual line break within paragraph. Tabs from line start to cursor are copied
after the soft-break (limited to defined tabstop count).

## Dependencies
- Phase 3.1c (Tabstops) — tab copying depends on tab blot and tabstop count

## Key References
- USE_CASE_ANALYSIS.md Feature 3
- Spike Item 7 (binding priority — addBinding appends to END)
- V24 source: `_patchKeyboard()` (Shift+Enter handler), SoftBreakBlot
- SECURITY: `innerHTML = '<br>'` → use `createElement('br')` + `appendChild()`

## Acceptance Criteria
- [ ] SoftBreakBlot registered (Embed, `<span class="ql-soft-break">` containing `<br>`)
- [ ] Shift+Enter keyboard binding with correct priority
- [ ] Tab copying logic: counts tabs before cursor, copies after soft-break
- [ ] Tab count limited to defined tabstop count
- [ ] Security: no innerHTML usage
- [ ] Playwright tests
