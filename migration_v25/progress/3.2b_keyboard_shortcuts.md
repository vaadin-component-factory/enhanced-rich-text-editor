# Phase 3.2b: Custom Keyboard Shortcuts (Feature 10)

## Status: NOT STARTED

## Feature Summary
`addStandardToolbarButtonShortcut()` binds key combos to toolbar button actions.
`addToolbarFocusShortcut()` binds key combo to focus toolbar.

## Dependencies
- Phase 3.1a (Custom Slots) — toolbar button references

## Key References
- USE_CASE_ANALYSIS.md Feature 10
- Spike Items: 6 (keyboard module accessible), 7 (binding priority)
- V24 source: `addStandardButtonBinding()`, `addToolbarFocusBinding()`
- **V25 change**: Use string key names, not numeric keyCodes (Quill 2)

## Acceptance Criteria
- [ ] `addStandardToolbarButtonShortcut()` Java API (string key names)
- [ ] `addToolbarFocusShortcut()` Java API
- [ ] Bindings injected via `this._editor.keyboard.addBinding()`
- [ ] Playwright tests
