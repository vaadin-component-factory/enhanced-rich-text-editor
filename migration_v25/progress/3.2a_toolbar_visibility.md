# Phase 3.2a: Toolbar Button Visibility (Feature 9)

## Status: NOT STARTED

## Feature Summary
Individual toolbar buttons can be shown/hidden via `setToolbarButtonsVisibility(Map)`.
Groups auto-hide when all buttons hidden. `ToolbarButton` enum (28 values).

## Dependencies
- Phase 3.1a (Custom Slots) — toolbar infrastructure

## Key References
- USE_CASE_ANALYSIS.md Feature 9
- V24 source: `_buttonDisplay()`, `_buttonGroupDisplay()`, ToolbarButton enum

## Acceptance Criteria
- [ ] `ToolbarButton` enum with all 28 values (standard + ERTE-specific)
- [ ] `setToolbarButtonsVisibility(Map<ToolbarButton, Boolean>)` Java API
- [ ] Groups auto-hide when all child buttons hidden
- [ ] CSS `display: none` applied per button via JS bridge
- [ ] Playwright tests
