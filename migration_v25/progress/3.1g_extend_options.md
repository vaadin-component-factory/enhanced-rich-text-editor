# Phase 3.1g: extendOptions Hook (Feature 16)

## Status: NOT STARTED

## Feature Summary
Pre-Quill-init callback: `window.Vaadin.Flow.vcfEnhancedRichTextEditor.extendOptions` array.
Each callback receives `(options, Quill)`. Critical for tables extension.

## Dependencies
- Phase 2 (ERTE Shell) — ready() lifecycle

## Key References
- USE_CASE_ANALYSIS.md Feature 16
- Spike Item 10 (module load order: blot registration → element definition → ready → _editor)
- V24 source: `ready()` method (lines 1137-1142)

## Acceptance Criteria
- [ ] `extendOptions` hook array checked in `ready()` before `super.ready()`
- [ ] Callbacks receive options object + Quill class
- [ ] Tables extension can register blots/modules via this hook
- [ ] Backward-compatible with ERTE 1 hook pattern
- [ ] Playwright tests
