# Phase 3.1c: Tabstops (Feature 1)

## Status: NOT STARTED

## Feature Summary
Tab key inserts TabBlot embed. Tab width calculated iteratively from defined tabstop positions.
L/R/M alignment. Java API: `TabStop` class, `setTabStops()`.

## Dependencies
- Phase 3.1a (Custom Slots) — toolbar infrastructure
- Phase 3.1b (Readonly) — sanitizer infrastructure (class whitelist)

## Key References
- USE_CASE_ANALYSIS.md Feature 1
- Spike Items: 2 (blot registration), 20 (guard nodes INSIDE embed — measure OUTER rect)
- V24 source: `blots.js` (TabBlot), `_updateTabWidths()`, `_requestTabUpdate()`

## Acceptance Criteria
- [ ] TabBlot registered (Embed, `<span class="ql-tab">`)
- [ ] Tab key inserts tab at cursor position
- [ ] Iterative width calculation engine ported (~200 lines)
- [ ] L/R/M alignment support
- [ ] Java `TabStop` class + `setTabStops()`/`getTabStops()` API
- [ ] Guard nodes handled (measure OUTER .ql-tab rect)
- [ ] Playwright tests (75 existing prototype tests as reference)
