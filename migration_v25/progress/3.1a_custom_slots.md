# Phase 3.1a: Custom Slots / Toolbar Slot System (Feature 8)

## Status: NOT STARTED

## Feature Summary
24 named `<slot>` elements in toolbar: START, END, BEFORE/AFTER for 10 groups + GROUP_CUSTOM.
Java API: `addToolbarComponents(slot, components)`, `ToolbarSlot` enum, `SlotUtil`.

## Dependencies
- Phase 2 (ERTE Shell) — COMPLETE

## Key References
- USE_CASE_ANALYSIS.md Feature 8
- Spike Items: 4 (render override), 5 (custom buttons survive re-renders)
- V24 source: `vcf-enhanced-rich-text-editor.html` (toolbar template), `SlotUtil.java`, `ToolbarSlot.java`

## Acceptance Criteria
- [ ] `render()` override adds 24 `<slot>` elements around standard toolbar groups
- [ ] Java `ToolbarSlot` enum with all 24 slot names
- [ ] `addToolbarComponents(slot, components...)` Java API
- [ ] Custom Vaadin components render in correct toolbar positions
- [ ] Standard RTE toolbar buttons unchanged
- [ ] Playwright tests for slot insertion and positioning
