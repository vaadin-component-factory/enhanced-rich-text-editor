# Phase 4.1: Tables Project Setup & Base

**Status:** NOT STARTED

## Objective

Set up the V25 Tables addon module structure, dependencies, and basic project infrastructure.

## Scope

1. **Maven Module Setup**
   - Create `enhanced-rich-text-editor-tables-v25/pom.xml`
   - Dependencies: V25 ERTE Core, Vaadin 25, Spring Boot 4
   - Version: 2.0.0-SNAPSHOT
   - License headers

2. **Package Structure**
   - `com.vaadin.componentfactory.richtexteditor.tables` — Java classes
   - `frontend/` — JS modules

3. **Base Classes**
   - `EnhancedRichTextEditorTables.java` — main component
   - Extends `EnhancedRichTextEditor` or composition?
   - Table configuration classes
   - Event classes

4. **Demo Integration**
   - Add tables module to demo `pom.xml`
   - Create `ErteTablesTestView.java`
   - Add to `ErteTestLayout` navigation

5. **Build Verification**
   - Module builds successfully
   - Demo includes tables module
   - No dependency conflicts

## Success Criteria

- [ ] Maven module builds cleanly
- [ ] Package structure follows V25 patterns
- [ ] License headers on all Java files
- [ ] Demo view accessible (empty shell OK)
- [ ] Reviewed by architecture-guard agent

## Architecture Decisions

**Composition vs. Inheritance:**
- Option A: `EnhancedRichTextEditorTables extends EnhancedRichTextEditor`
- Option B: Separate component with ERTE as child
- Decision: TBD during planning (likely composition for separation of concerns)

**JS Module Structure:**
- Option A: Single `vcf-enhanced-rich-text-editor-tables.js` extending ERTE
- Option B: Separate table blot modules imported by ERTE
- Decision: TBD during planning

## Resources

- V24 tables module (`enhanced-rich-text-editor-tables/`)
- V25 core module structure (`enhanced-rich-text-editor-v25/`)
- Vaadin Tables addon (if exists in Vaadin 25)

## Output

- `enhanced-rich-text-editor-tables-v25/` module structure
- Basic Java classes (shells)
- Demo view (empty)
- Build configuration
