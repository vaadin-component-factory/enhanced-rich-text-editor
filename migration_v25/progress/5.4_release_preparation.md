# Phase 5.4: Release Preparation

## Status: IN PROGRESS (5.4a COMPLETE, 5.4b–c NOT STARTED)

## Scope

Prepare the V25 migration for release as ERTE 6.0.0.

**IMPORTANT:** All work stays on the `v25` branch. No merging to `master` or any other branch.

### 5.4a — Module Settings Audit ✅ COMPLETE
Added `directory` Maven profile and `assembly/` directory to both V25 modules, matching V24 release infrastructure:
- `enhanced-rich-text-editor-v25/assembly/assembly.xml` + `MANIFEST.MF`
- `enhanced-rich-text-editor-tables-v25/assembly/assembly.xml` + `MANIFEST.MF`
- `directory` profile in both V25 POMs (assembly-plugin 3.8.0, source-plugin 3.3.1, javadoc-plugin 3.7.0)
- Verified: `mvn install -Pdirectory` produces correct zips (JAR + sources + javadoc + LICENSE + README + MANIFEST.MF)
- Nice-to-have gaps (sample.css, template-dialog-sample.json) intentionally skipped — documented in V25 guides

### 5.4b — Remove V24 Code
Remove legacy V24 modules that are excluded from build but still in the repo:
- `enhanced-rich-text-editor/` (V24 core)
- `enhanced-rich-text-editor-tables/` (V24 tables)
- Clean up root POM references
- Clean up any remaining V24 cross-references in docs

### 5.4c — Rename V25 Modules (Remove `-v25` Suffix)
Rename modules to their final release names:
- `enhanced-rich-text-editor-v25/` → `enhanced-rich-text-editor/`
- `enhanced-rich-text-editor-tables-v25/` → `enhanced-rich-text-editor-tables/`
- Update all POM artifactIds, module references, cross-references
- Update all doc references, README, CLAUDE.md

**Order:** 5.4b before 5.4c (remove V24 first, then rename V25 into the freed names).

## Verification
- `bash v25-build.sh` succeeds
- `bash v25-build-it.sh` succeeds
- All 306 ERTE tests pass
- All doc cross-references resolve
- No remaining `-v25` suffixes in artifact names
